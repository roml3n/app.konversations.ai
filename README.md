
  # Konversations Prototype

  This is a code bundle for Konversations Prototype. The original project is available at https://www.figma.com/design/VG4cywiU1tFRoOOIzUI4eK/Konversations-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  